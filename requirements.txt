# Core runtime — required.
# Install into a project-local virtual environment with:  bash setup.sh
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
pytest>=8.0

# ── Optional extras ───────────────────────────────────────────────────────────
# Neither is needed to run the simulator, the experiments or the animation.
# Install with:  bash setup.sh --gpu --dev
#
#   vispy, PyOpenGL       GPU renderer (vision/vispy_renderer.py). Without them
#                         tests/test_vispy.py skips; nothing else is affected.
#   opencv-python-headless
#                         reference implementation used by a handful of tests to
#                         cross-check EPnP and the LM refiner. Without it those
#                         tests skip. NOT used at runtime.
#
# vispy>=0.14
# PyOpenGL>=3.1
# opencv-python-headless>=4.8
